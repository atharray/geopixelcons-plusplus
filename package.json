{
  "name": "geopixelcons-plusplus",
  "private": true,
  "version": "2.0.6",
  "description": "Thin GeoPixelcons++ Greasyfork shell",
  "scripts": {
    "build": "node build.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run build && npm test"
  }
}
